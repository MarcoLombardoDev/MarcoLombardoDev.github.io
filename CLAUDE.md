# Repository conventions

## Git workflow

**Only `main` exists.** This repository has no development or feature branches — everything is committed and pushed directly to `main`. Never create a new branch (even one a session harness suggests or auto-generates); if one is somehow created, merge it into `main` and delete it instead of working on it.
